# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "2.5"
metadata:
  name: zsap.com.r3.cobi.s4.comaccigb
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: http://l2vs2slass01.mef.gov.it:8000
          client: "100"
        credentials:
          username: env:USER
          password: env:PSW
        app:
          name: Z_S4_COMACCIGB
          description: Comunicazione Accantonamenti IGB
          package: Z_ALM_S4_COBI_FIORI
          transport: 
